import { Text } from 'react-native';

import { DefaultContainer } from '@/components/ui';

const Profile = () => {
  return (
    <DefaultContainer showTabBar>
      <Text>Perfil</Text>
    </DefaultContainer>
  );
};

export default Profile;
